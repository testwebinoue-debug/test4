
  # Convert Frames to Responsive HTML

  This is a code bundle for Convert Frames to Responsive HTML. The original project is available at https://www.figma.com/design/r4aEfnY8Tz2BwqVV60m8nX/Convert-Frames-to-Responsive-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  